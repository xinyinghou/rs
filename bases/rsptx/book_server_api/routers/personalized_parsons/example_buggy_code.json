{"Problem Name":"table_reservation_oc",
"CF (Code)":"def table_reservation(reservation_dict, guest_num):    count = 0    for key, value in reservation_dict.items():        for item in value:            for key,value in item.values():                if value == guest_num:                    count += 1    return count"
}

